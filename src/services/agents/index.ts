
// Export main components
export { MultiAgentSystem } from './core/MultiAgentSystem';
// Export from the main hooks directory instead of local hooks
export { useMultiAgentSystem } from '@/hooks/useMultiAgentSystem';

// Re-export types from the types directory
export * from './types';
